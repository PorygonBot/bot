interface Rules {
    channelId: string,
	leagueName: string,
	recoil: string,
	suicide: string,
	abilityitem: string,
	selfteam: string,
	db: string,
	spoiler: Boolean,
	ping: string,
	forfeit: string,
	format: string,
	quirks: Boolean,
	stopTalking: Boolean,
	tb: Boolean,
	combine: Boolean,
	redirect: string,
	timeOfPing: string
}

export default Rules